#include "cosmosants.h"

vector<CosmoSantsApartment> CosmoSantsAPI::findApartments(int persons)
{
    vector<CosmoSantsApartment> apts;

    apts.push_back({55});
    apts.push_back({65});
    apts.push_back({135});

    return apts;
}